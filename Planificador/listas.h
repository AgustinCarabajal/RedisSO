#ifndef LISTAS
#define LISTAS

	#include <stdio.h>
	#include <stdlib.h>
	#include "pcblib.h"
	

	pcb *ready = NULL;
 	pcb *locked= NULL;
 	pcb *incoming = NULL;
 	pcb *running = NULL;	

#endif